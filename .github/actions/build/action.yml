name: build
description: Build the Firefox and Chrome apps
runs:
  using: "composite"
  steps:
    - name: Build Firefox app
      shell: bash
      run: yarn build:firefox

    - name: Build Chrome app
      shell: bash
      run: yarn build:chrome
